name: trail_app
description: Application mobile pour le suivi de trail
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  geolocator: ^9.0.2
  sensors_plus: ^2.0.0
  mqtt_client: ^9.6.1
  battery_plus: ^2.0.3
  torch_light: ^0.4.0
  audioplayers: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/alert.mp3
